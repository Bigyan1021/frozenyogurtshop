apiVersion: apps/v1
kind: Deployment
metadata:
  name: frozenwebapp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frozen-web-app
  template:
    metadata:
      labels:
        app: frozen-web-app
    spec:
      containers: 
        - name: frozen-web-app
          image: bigyankc1021/frozenwebapp:main
          ports:
            - containerPort: 80
          resources:
            requests:
              cpu: "5m"
              memory: "10Mi"
            limits:
              cpu: "30m"
              memory: "30Mi"
