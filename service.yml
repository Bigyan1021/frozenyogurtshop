apiVersion: v1
kind: Service
metadata:
  name: frozen-webapp
spec:
  type: NodePort
  selector:
    app: frozen-web-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080
